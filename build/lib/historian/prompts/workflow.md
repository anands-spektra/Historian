You are documenting WORKFLOW.md — the end-to-end development lifecycle of this
project, for a teammate who wants to understand how work flows through it. Base
everything ONLY on the context below.

Output a complete Markdown document starting with a top-level "# Workflow" title.
Include a Mermaid flowchart of the lifecycle (valid syntax), then explain each
stage in prose.

The lifecycle to explain (adapt to what the context actually shows):

1. User request
2. Claude implementation (files change)
3. Automation detects the iteration
4. Analysis by the model provider
5. Documentation update (implementation.md)
6. Final architecture generation

For each stage: what triggers it, what it does, what it produces, and where that
output lives. Also describe how a developer runs, inspects (status), and finalizes
the system in practice.

=======================================================================
CONTEXT
=======================================================================
[[CONTEXT]]
